
#include <iostream>

using namespace std;

int main()
{
    string str,res=" ";
    cin>>str;
    int i,j;
    for(i=0;i<str.size();i++){
        for(j=0;j<str.size();j++){
            if(str[i]==str[j]){
                break;
            }
        }if(i==j){
            res+=str[i];
        }
    }
    cout<<res;

    return 0;
}
